# Simple Live Settings Guide

## 零、文档范围与阅读方式

### 0.1 适用版本

本文说明 Simple Live 主 App“其他设置”中的播放器、桌面窗口、日志和配置包选项，也说明 Windows GPU 偏好、TV 关注刷新、直播卡顿恢复、通知和锁定控制的相关行为。设置名称以当前界面为准；不同平台会隐藏不适用的选项。

### 0.2 先记住三条规则

- 改动 mpv 参数后，通常要退出当前直播间并重新进入；如果修改的是 Windows GPU 偏好，必须完全退出并重新启动应用。
- `硬件加速`、`--hwdec`、Windows GPU 偏好是三个不同层次：一个控制 Flutter 视频纹理渲染，一个控制视频解码器，一个控制 Flutter 引擎优先使用哪类显卡。
- 导入配置前先导出当前配置包。配置包包含设置、关注、屏蔽词和账号信息，分享前请检查 Cookie 等隐私内容。

## 一、进入“其他设置”

### 1.1 打开页面

在主 App 的“设置”页面选择“其他设置”。Windows、macOS、Linux 会显示桌面窗口和 Windows GPU 相关选项；Android、iOS 只显示播放器、日志和平台适用的选项。

### 1.2 配置包操作

#### 1.2.1 导出配置包

“导出配置包”会生成 `simple_live_profile.json`。它适合迁移设置或备份。导出的设置使用本地 Hive 内容，不能把正在运行的播放器状态一起迁移。

#### 1.2.2 导入配置包

“导入配置包”支持覆盖导入和合并导入：覆盖会先清空可导入设置，合并会保留本机已有设置。导入后应用会重新加载设置并刷新关注数据。导入陌生文件前检查 JSON 来源，避免覆盖账号和隐私配置。

#### 1.2.3 重置配置

“重置配置”清理本地设置、弹幕屏蔽词和部分缓存状态。重置后按提示重启应用；关注列表、历史和账号是否保留取决于当前版本的导入范围。

## 二、桌面窗口设置

### 2.1 记住窗口大小和位置

启用后，Windows、macOS、Linux 会在下次启动恢复上次普通窗口的位置、大小和最大化状态。关闭后应用仍会使用默认窗口尺寸。

### 2.2 桌面直播间标题

桌面直播间使用播放器覆盖层而不是传统 AppBar，因此标题会叠加在视频区域上方。标题来自直播间详情；详情尚未返回时显示“直播间”。标题过长会省略，不影响视频和聊天区操作。

## 三、mpv 性能档位

### 3.1 流畅

“流畅”使用较轻量的缩放算法、`hwdec=auto-safe` 和较少的画质处理，适合核显、低功耗笔记本、远程桌面或 CPU 占用较高的设备。优先目标是减少掉帧和风扇噪音。

### 3.2 均衡

“均衡”是默认档位。Windows 使用系统和播放器默认的平衡策略，macOS、Linux 使用桌面端的 `gpu-hq` 基础参数。它适合大多数桌面设备和日常直播。

### 3.3 画质

“画质”启用更高质量的缩放、`gpu-next`（平台支持时）和去色带处理。它会增加 GPU 显存、功耗和延迟，适合独显、台式机或高分辨率原画。若出现黑屏、花屏或播放卡顿，先退回“均衡”。

## 四、自定义输出与硬件解码

### 4.1 自定义输出驱动与硬件加速

开启后，界面选择的 `--vo`、`--ao`、`--hwdec` 才会覆盖档位中的对应值；关闭后仍会使用档位和播放器默认值。修改后重开直播间。

“硬件加速”是 `VideoControllerConfiguration.enableHardwareAcceleration`，主要影响 Flutter 视频纹理的渲染路径。它不是“强制 NVIDIA 解码”开关；显卡选择请使用 Windows GPU 偏好或系统 PRIME 设置。

### 4.2 高级 mpv options

每行写一个 `key=value` 或 `key value`，可以带 `--` 前缀，`#` 后面的内容会视为注释。例如：

```text
profile=gpu-hq
hwdec=auto-safe
video-sync=display-resample
scale=spline36
```

拼写错误、当前平台不支持或播放器拒绝的选项会被记录到日志，不会阻止直播间打开。不要把完整 shell 命令粘贴进这里。

### 4.3 导入 mpv.conf

“导入 mpv.conf”会把文件复制到应用私有目录。当前解析器支持简单的 `key=value` / `key value` 行和 `#` 注释；复杂的 section、include、Lua 脚本不会按完整 mpv 配置语法执行。导入后重开直播间。

### 4.4 参数覆盖顺序

#### 4.4.1 最终生效顺序

同名参数按以下顺序覆盖：内置性能档位 → 可视化自定义输出 → 高级 mpv options → 导入的 `mpv.conf`。后出现的值优先。

#### 4.4.2 iOS 差异

iOS 的 `media_kit` 原生播放器不会应用 `applyToPlayer` 中的通用 mpv property；iOS 只使用视频控制器支持的硬件解码和纹理配置。因此不要用导入 `mpv.conf` 期待改变 iOS 的 `vo` 或音频驱动。

#### 4.4.3 桌面 `--vo` 差异

Windows、macOS、Linux 的视频画面由 `media_kit_video` 的 Flutter 原生表面承载，`--vo` 不会像命令行 mpv 那样直接替换 Flutter 表面。桌面端优先调“性能档位”、硬件解码和 GPU 偏好。

## 五、驱动选项说明

### 5.1 视频输出驱动 `--vo`

`gpu`、`gpu-next` 是常见桌面渲染选择；`xv`、`x11`、`vdpau` 只适合特定 X11 环境；`direct3d` 仅 Windows；`mediacodec_embed` 仅 Android 兼容路径。未知驱动不要随意尝试。

### 5.2 音频输出驱动 `--ao`

Windows 通常使用 `wasapi`；Linux 优先 `pipewire` 或 `pulse`；macOS 使用 `coreaudio`；iOS 使用 `audiounit`；Android 使用 `audiotrack` 或 `aaudio`。出现无声时先关闭“自定义输出驱动”，再检查系统音量和静音按钮。

### 5.3 硬件解码器 `--hwdec`

`auto-safe` 是推荐起点，`auto` 允许播放器更积极地选择硬解。`d3d11va` / `nvdec` 主要用于 Windows，`videotoolbox` 用于 Apple 平台，`vaapi` / `vdpau` 用于 Linux，`mediacodec` 用于 Android。`*-copy` 会把解码结果复制回系统内存，兼容性更好但占用更高。

## 六、Windows GPU 偏好

### 6.1 三个选项

#### 6.1.1 自动

使用 Flutter 和 Windows 的默认 GPU 选择。适合台式机或不确定显卡驱动状态的设备。

#### 6.1.2 省电/核显

请求 Flutter 引擎优先使用低功耗适配器。适合电池续航优先、只看低清直播或希望降低温度的笔记本。

#### 6.1.3 高性能/NVIDIA 独显

请求 Flutter 引擎优先使用高性能适配器。游戏本、双 GPU 笔记本和 NVIDIA 独显误用核显时优先选择此项，然后完全退出并重启 Simple Live。

### 6.2 选择范围与限制

该设置使用 Flutter Windows 的 `NoPreference`、`LowPowerPreference`、`HighPerformancePreference`。它表达“偏好”，不是按适配器名称锁定某一块显卡；Windows 驱动仍可能回退。`media_kit_video` 的 ANGLE/纹理设备也会独立选择适配器，因此不能保证 mpv、Flutter 和浏览器都使用同一块卡。

macOS 没有同等的 Flutter 选择器，通常由系统自动切换；Linux 使用 Flutter/EGL 启动环境，需通过 `DRI_PRIME`、NVIDIA PRIME 或桌面启动器配置。不要在 macOS/Linux 界面中寻找 Windows GPU 选项。

### 6.3 推荐排查顺序

1. Windows 设置选择“高性能/NVIDIA 独显”，完全退出应用后重新打开。
2. 选择“均衡”或“流畅”，确认 `--hwdec=auto-safe`，避免同时强制多个实验性选项。
3. 在 NVIDIA 控制面板的“程序设置”中为 `simple_live_app.exe` 选择高性能 NVIDIA 处理器。
4. 仍然错误时打开日志，记录显卡驱动版本、播放器诊断和实际硬件解码器。

## 七、日志记录

### 7.1 开启日志记录

只在排查问题时开启。日志会记录网络、播放器、解码器和设置应用过程，可能包含直播地址、房间号和错误细节。复现后关闭日志，使用“日志列表”保存需要的文件。

### 7.2 日志列表与清空

“日志列表”显示应用支持目录中的日志文件；桌面可以保存或分享，Linux 可能没有系统分享入口。“清空日志”前先关闭日志记录，否则应用会提示先关闭。

## 八、按设备推荐配置

### 8.1 低功耗核显笔记本

#### 8.1.1 建议值

- mpv 性能档位：`流畅`
- 自定义输出：关闭
- 硬件解码器：`auto-safe`
- Windows GPU 偏好：`省电/核显`
- 遇到花屏时：先把 `硬件加速` 关闭测试

### 8.2 双 GPU 游戏本

#### 8.2.1 建议值

- mpv 性能档位：`均衡`，画质要求高再选 `画质`
- Windows GPU 偏好：`高性能/NVIDIA 独显`
- 硬件解码器：先 `auto-safe`，确认稳定后再试 `d3d11va` 或 `nvdec`
- 插电时同时在 NVIDIA 控制面板指定高性能 GPU

### 8.3 台式机或高端独显

#### 8.3.1 建议值

- mpv 性能档位：`均衡` 或 `画质`
- GPU 偏好：`自动` 或 `高性能`
- 画面流畅优先时关闭 `deband` 等高负载自定义项
- 出现高显存时把封面显示限制在预览尺寸，不要导入原始 4K 封面配置

### 8.4 远程桌面、虚拟机和 Linux 老设备

#### 8.4.1 建议值

- mpv 性能档位：`流畅`
- 自定义输出：关闭
- Linux 先确认 `pipewire` / `pulse` 和 `DRI_PRIME` 环境
- 远程桌面花屏时禁用硬件加速并重启直播间

### 8.5 iPhone、iPad

#### 8.5.1 建议值

- 保持默认的硬件解码和 iOS 原画省电优化。
- 不要依赖桌面 mpv.conf 的 `vo`、`ao` 参数。
- 当前 `floating` 插件明确是 Android-only，iOS 系统画中画需要 AVPlayer/原生 PiP 管线；本项目当前 `media_kit` 播放链路尚未提供可安全复用的 iOS PiP 输出，因此 iOS PiP 仍显示为不可用。

## 九、常见问题

### 9.1 直播卡住但没有错误提示

播放器现在会在 `playing=true` 且播放位置连续停滞约 15 秒时触发有限次数的自动刷新；持续 buffering 会使用更长的 30 秒窗口。刷新经过当前房间、线路和媒体代次校验，不会让旧房间的重试覆盖新房间。超过上限后需要手动刷新或切换线路。

### 9.2 直播间锁定后看不到解锁按钮

锁定播放器后，Android、iOS、macOS、Linux、Windows 都可以先轻触画面显示边缘解锁按钮，再点击按钮解除锁定。桌面端也保留鼠标移到左右边缘显示按钮的方式。这里的“锁定”是播放器控制锁，不是系统锁屏；系统物理锁屏后的唤醒由操作系统决定。

### 9.3 特别关注通知没有出现

通知需要系统权限，首次启用特别关注或启动已有特别关注时会请求权限。Android/iOS 本地通知可以在 Dart 进程仍存活且后台刷新成功时显示；系统强制结束应用后，本地代码无法继续轮询。要实现真正的“应用被强制关闭仍能通知”，还需要服务端监测各直播平台并接入 FCM/APNs 推送，不能仅靠 `media_kit` 或本地定时器解决。

### 9.4 TV 进入主页后关注状态没有立即变化

TV 在服务启动后先加载本地关注列表；当“定时刷新关注状态”开启时，会在主页显示前主动执行一轮全量状态刷新。之后仍按设置的周期刷新。进入“我的关注”页的独立刷新开关只控制再次进入页面的行为。

### 9.5 封面显示导致 Windows 内存上涨

关注页的直播封面会使用预览尺寸解码、独立的内存缓存和销毁时淘汰策略，避免把主播原始大图长期留在全局 ImageCache。若旧版本已经占用大量内存，关闭窗口后重新启动一次即可释放旧进程内存。

## 十、修改后验证清单

### 10.1 播放器设置

1. 改一个 mpv 参数并重新进入直播间。
2. 打开播放诊断，确认实际硬件解码器、视频输出和分辨率。
3. 出现异常时关闭自定义输出，回到“均衡 + auto-safe”。

### 10.2 Windows 双 GPU

1. 选择“高性能/NVIDIA 独显”。
2. 完全退出并重新启动应用，而不是只返回主页。
3. 用 NVIDIA 控制面板和日志交叉确认；不要只根据任务管理器瞬时占用判断。

### 10.3 导入导出

1. 导出 JSON 备份。
2. 导入到测试实例，确认关注、屏蔽词和播放器设置。
3. 检查账号 Cookie 后再分享文件。